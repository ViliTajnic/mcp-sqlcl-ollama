# --- Ollama (OpenAI-compatible) ---
OLLAMA_BASE_URL=http://127.0.0.1:11434/v1
OLLAMA_API_KEY=ollama
OLLAMA_MODEL=gpt-oss:20b

# --- SQLcl binary path (use just 'sql' if on PATH) ---
SQLCL_BIN=sql

# Optional: default SQLcl connection name to auto-connect via MCP 'connect'
SQLCL_CONN_NAME=mcp23ai
